/**
 * Type for object that represent an attribute (e.g oxygen) 
 *
 * @typedef Attribute
 * @type object
 * @property {Rule} Rule
 * @property {string} name - The name of the attribute
 * @property {string} color - Base color of the Attribute
 * @property {float} max - The maximal amount the cell can hold, e.g 100%
 * @property {float} cornucopia - The current cornucopia of the attribute, e.g 50%
 */