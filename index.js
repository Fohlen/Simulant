var Cell = require('./Cell');
var Room = require('./Room');
var Automat = require('./Automat');

var elements = [];
var c = new Cell();

// Do a 2-dimensional simulation for 100 cells
for (var i = 0; i < 100; i++) {
    elements.push[c];
}

var r = new Room(2);
var a = new Automat(elements, room);
a.loop();
